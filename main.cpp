#include "sdt.h"

//This program is just an example one under VCS/
int main(){
  int a,b;
  cout << "Enter A and B: ";
  cln >> a >> b;
  cout << "A + B = " << a + b << '\n'
       << "A + B = " << a - b << '\n'
       << "A * B = " << a * b << '\n';
}
